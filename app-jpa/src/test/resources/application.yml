# 스프링 부트가 별다른 설정이 없으면 메모리모드로 실행한다.
#spring:
#  datasource:
#    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:testdb
#    username: sa
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        show_sql: true # system.out 출력
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug